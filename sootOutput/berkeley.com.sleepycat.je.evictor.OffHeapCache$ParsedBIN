    void <init>(int, long[], long, long, int, java.nio.ByteBuffer)
    {
        berkeley.com.sleepycat.je.evictor.OffHeapCache$ParsedBIN this;
        int flags, minExpiration;
        long[] lnMemIds;
        long lastFullLsn, lastDeltaLsn;
        java.nio.ByteBuffer binBytes;

        this := @this: berkeley.com.sleepycat.je.evictor.OffHeapCache$ParsedBIN;

        flags := @parameter0: int;

        lnMemIds := @parameter1: long[];

        lastFullLsn := @parameter2: long;

        lastDeltaLsn := @parameter3: long;

        minExpiration := @parameter4: int;

        binBytes := @parameter5: java.nio.ByteBuffer;

        specialinvoke this.<java.lang.Object: void <init>()>();

        this.<berkeley.com.sleepycat.je.evictor.OffHeapCache$ParsedBIN: int flags> = flags;

        this.<berkeley.com.sleepycat.je.evictor.OffHeapCache$ParsedBIN: long[] lnMemIds> = lnMemIds;

        this.<berkeley.com.sleepycat.je.evictor.OffHeapCache$ParsedBIN: long lastFullLsn> = lastFullLsn;

        this.<berkeley.com.sleepycat.je.evictor.OffHeapCache$ParsedBIN: long lastDeltaLsn> = lastDeltaLsn;

        this.<berkeley.com.sleepycat.je.evictor.OffHeapCache$ParsedBIN: int minExpiration> = minExpiration;

        this.<berkeley.com.sleepycat.je.evictor.OffHeapCache$ParsedBIN: java.nio.ByteBuffer binBytes> = binBytes;

        return;
    }


