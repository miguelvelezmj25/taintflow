    static void <clinit>()
    {
        optimizer.com.googlecode.pngtastic.core.processing.zopfli.Options$BlockSplitting[] $r0;
        int $i0, $i1, $i2, $i3;
        int[] $r1, $r2, $r5, $r8;
        optimizer.com.googlecode.pngtastic.core.processing.zopfli.Options$BlockSplitting $r3, $r6, $r9;
        java.lang.NoSuchFieldError $r4, l0#9, $r7, l0#15, $r10, l0#21;

        $r0 = staticinvoke <optimizer.com.googlecode.pngtastic.core.processing.zopfli.Options$BlockSplitting: optimizer.com.googlecode.pngtastic.core.processing.zopfli.Options$BlockSplitting[] values()>();

        $i0 = lengthof $r0;

        $r1 = newarray (int)[$i0];

        <optimizer.com.googlecode.pngtastic.core.processing.zopfli.Deflate$1: int[] $SwitchMap$optimizer$com$googlecode$pngtastic$core$processing$zopfli$Options$BlockSplitting> = $r1;

     label01:
        $r2 = <optimizer.com.googlecode.pngtastic.core.processing.zopfli.Deflate$1: int[] $SwitchMap$optimizer$com$googlecode$pngtastic$core$processing$zopfli$Options$BlockSplitting>;

        $r3 = <optimizer.com.googlecode.pngtastic.core.processing.zopfli.Options$BlockSplitting: optimizer.com.googlecode.pngtastic.core.processing.zopfli.Options$BlockSplitting FIRST>;

        $i1 = virtualinvoke $r3.<optimizer.com.googlecode.pngtastic.core.processing.zopfli.Options$BlockSplitting: int ordinal()>();

        $r2[$i1] = 1;

     label02:
        goto label04;

     label03:
        $r4 := @caughtexception;

        l0#9 = $r4;

     label04:
        $r5 = <optimizer.com.googlecode.pngtastic.core.processing.zopfli.Deflate$1: int[] $SwitchMap$optimizer$com$googlecode$pngtastic$core$processing$zopfli$Options$BlockSplitting>;

        $r6 = <optimizer.com.googlecode.pngtastic.core.processing.zopfli.Options$BlockSplitting: optimizer.com.googlecode.pngtastic.core.processing.zopfli.Options$BlockSplitting LAST>;

        $i2 = virtualinvoke $r6.<optimizer.com.googlecode.pngtastic.core.processing.zopfli.Options$BlockSplitting: int ordinal()>();

        $r5[$i2] = 2;

     label05:
        goto label07;

     label06:
        $r7 := @caughtexception;

        l0#15 = $r7;

     label07:
        $r8 = <optimizer.com.googlecode.pngtastic.core.processing.zopfli.Deflate$1: int[] $SwitchMap$optimizer$com$googlecode$pngtastic$core$processing$zopfli$Options$BlockSplitting>;

        $r9 = <optimizer.com.googlecode.pngtastic.core.processing.zopfli.Options$BlockSplitting: optimizer.com.googlecode.pngtastic.core.processing.zopfli.Options$BlockSplitting NONE>;

        $i3 = virtualinvoke $r9.<optimizer.com.googlecode.pngtastic.core.processing.zopfli.Options$BlockSplitting: int ordinal()>();

        $r8[$i3] = 3;

     label08:
        goto label10;

     label09:
        $r10 := @caughtexception;

        l0#21 = $r10;

     label10:
        return;

        catch java.lang.NoSuchFieldError from label01 to label02 with label03;
        catch java.lang.NoSuchFieldError from label04 to label05 with label06;
        catch java.lang.NoSuchFieldError from label07 to label08 with label09;
    }


